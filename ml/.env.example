# Hugging Face token with *write* scope — only needed for `push_to_hub.py`.
# Create at https://huggingface.co/settings/tokens . Never commit the real value.
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Target model repo on the Hub (namespace/name).
MODEL_REPO=yourusername/emotion-distilbert
